# Observability

Thư mục này dùng để thêm OpenTelemetry collector, dashboards, và alert rules.

Đề xuất bước tiếp theo:
- log correlation với `X-Request-Id`
- trace span cho đường đi Gateway -> AI Engine -> Qdrant -> Gemini
- metric quota/rate-limit theo `project_id`
